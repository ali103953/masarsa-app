{
  "name": "masarsa-app",
  "version": "1.0.0",
  "description": "MASARSA school transportation web shell utilities",
  "private": true,
  "scripts": {
    "build": "echo \"Flutter is the production iOS build path; no web build step is configured.\"",
    "cap:add:ios": "npx cap add ios",
    "cap:sync": "npx cap sync"
  },
  "dependencies": {
    "@capacitor/core": "5.7.0",
    "@capacitor/ios": "5.7.0"
  },
  "devDependencies": {
    "@capacitor/cli": "5.7.0"
  }
}
